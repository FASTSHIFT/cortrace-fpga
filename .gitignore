# Vivado build artifacts
build/
*.jou
*.log
*.bit
*.ltx
.Xil/
vivado*.str
usage_statistics_webtalk.*
*.pb

# captures / traces (large, regenerate on demand)
*.bin
*.perftrace
captures/
perftrace/

# python
__pycache__/
*.pyc

# ARM reference PDFs (licensed; keep local, not in repo)
docs/history/refs/*.pdf

# compiled host helpers (build from source via host/scripts/Makefile)
host/scripts/stream_grab
*.gen/
